{
  "name": "@heroor/npmm",
  "version": "0.1.0",
  "author": "Bener",
  "license": "MIT",
  "main": "./dist/index.mjs",
  "type": "module",
  "bin": {
    "n": "./bin/n",
    "npmm": "./bin/n"
  },
  "scripts": {
    "build": "tsdown",
    "prepack": "tsdown",
    "typecheck": "tsc --noEmit",
    "test": "tsdown && node --test test/*.test.mjs",
    "dev": "tsdown -w"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "tsdown": "^0.22.0",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "files": [
    "dist",
    "bin"
  ],
  "dependencies": {
    "@clack/prompts": "1.2.0"
  }
}
